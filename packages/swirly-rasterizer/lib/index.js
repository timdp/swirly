const { createHtml } = require('./html')
const { rasterizeSvg } = require('./rasterize')
const { Screenshotter } = require('./screenshot')

module.exports = {
  createHtml,
  rasterizeSvg,
  Screenshotter
}
