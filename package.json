{
  "name": "swirly",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "test": "lerna run test",
    "lint": "yarn run lint:js && yarn run lint:deps",
    "lint:js": "standardx \"**/*.{js,ts}\"",
    "lint:deps": "scripts/depcheck.sh",
    "format": "prettier-standard \"**/*.{js,ts}\" && standardx --fix \"**/*.{js,ts}\"",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.24",
    "@swc/core": "^1.1.48",
    "@typescript-eslint/eslint-plugin": "^2.34.0",
    "@typescript-eslint/parser": "^2.34.0",
    "depcheck": "^0.9.2",
    "eslint-plugin-simple-import-sort": "^5.0.3",
    "husky": "^4.2.5",
    "lerna": "^3.21.0",
    "lint-staged": "^10.2.4",
    "prettier-standard": "^16.3.0",
    "standardx": "^5.0.0",
    "typescript": "^3.9.3"
  },
  "standardx": {
    "parser": "@typescript-eslint/parser",
    "plugins": [
      "@typescript-eslint/eslint-plugin"
    ],
    "globals": [
      "Image",
      "DOMParser",
      "XMLSerializer"
    ]
  },
  "eslintConfig": {
    "plugins": [
      "simple-import-sort"
    ],
    "rules": {
      "no-unused-vars": "off",
      "simple-import-sort/sort": "error"
    }
  },
  "lint-staged": {
    "*.{js,ts}": [
      "prettier-standard",
      "standardx --fix"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged && yarn run lint && yarn run build && yarn run test && scripts/build-examples.sh"
    }
  },
  "workspaces": [
    "packages/*"
  ]
}
